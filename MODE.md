# Режимы работы Temka Client

## 🟢 Локальный режим (по умолчанию)

Сайт работает **без сервера**, используя только браузер и localStorage.

**Преимущества:**
- ✅ Работает сразу, без установки
- ✅ Не нужен Node.js
- ✅ Быстро и просто
- ✅ Идеально для демо и тестирования

**Недостатки:**
- ❌ Данные хранятся только в браузере
- ❌ Данные теряются при очистке кэша
- ❌ Не работает между разными браузерами

**Как использовать:**
1. Просто откройи `index.html` в браузере
2. Всё работает!

---

## 🔵 Серверный режим (с Node.js)

Сайт работает с реальным сервером и базой данных SQLite.

**Преимущества:**
- ✅ Данные сохраняются на сервере
- ✅ Работает между разными браузерами
- ✅ Более безопасно
- ✅ Готово к production

**Недостатки:**
- ❌ Нужен Node.js
- ❌ Нужно запускать сервер

**Как использовать:**

1. Установи зависимости:
```bash
npm install
```

2. Запусти сервер:
```bash
npm start
```

3. Откройи http://localhost:3000

4. Измени `index.html` - замени:
```html
<script src="api-local.js"></script>
```
на:
```html
<script src="api.js"></script>
```

---

## 📊 Сравнение режимов

| Функция | Локальный | Серверный |
|---------|-----------|-----------|
| Работает без установки | ✅ | ❌ |
| Сохраняет данные | ❌ | ✅ |
| Работает между браузерами | ❌ | ✅ |
| Нужен сервер | ❌ | ✅ |
| Для production | ❌ | ✅ |

---

## 🚀 Рекомендации

- **Для демо/тестирования:** Локальный режим
- **Для разработки:** Серверный режим
- **Для production:** Серверный режим на облачном хостинге

---

## 🔄 Переключение режимов

### Локальный → Серверный

В `index.html` замени:
```html
<!-- Было -->
<script src="api-local.js"></script>

<!-- Стало -->
<script src="api.js"></script>
```

### Серверный → Локальный

В `index.html` замени:
```html
<!-- Было -->
<script src="api.js"></script>

<!-- Стало -->
<script src="api-local.js"></script>
```

---

## 💾 Экспорт данных

Если хочешь сохранить данные из локального режима:

```javascript
// В консоли браузера
const users = JSON.parse(localStorage.getItem('temka_users'));
const downloads = JSON.parse(localStorage.getItem('temka_downloads'));
console.log('Users:', users);
console.log('Downloads:', downloads);
```

Скопируй вывод и сохрани в файл.
